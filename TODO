4. Support decryption of ts
Handle non-base64 AES key
-> Need to load the AES key somewhere in session.cpp so that
we can use a different method for kodi and the tests
-> support multiple EXT-X-KEY segments
-> support optional IV values

1. Support rest of addon interface
2. Be able to switch to a variant playlist during playing
3. Support a live stream


Issues
1. Current time seems to be incorrect

Missing Support
-Tons